# Django security — generate a new secret key for every environment
# python3 -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=your-secret-key-here

DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# PostgreSQL
DB_NAME=coaching_db
DB_USER=coaching_user
DB_PASSWORD=your-db-password-here
DB_HOST=db
DB_PORT=5432

# CORS — list your frontend URLs
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

DJANGO_LOG_LEVEL=WARNING

# Email — use SMTP for production, leave as console for local dev
# For Gmail: create an App Password (Google Account > App Passwords)
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-gmail-address@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password-here
DEFAULT_FROM_EMAIL=Football Academy <noreply@yourdomain.com>
ADMIN_NOTIFICATION_EMAIL=admin@yourdomain.com

# Frontend URL — used in email verification and password reset links
FRONTEND_URL=https://yourdomain.com

# Redis / Celery — required for async email delivery
# Install Redis: https://redis.io/docs/getting-started/
# Start Redis:  redis-server
# Start worker: celery -A config worker --loglevel=info
REDIS_URL=redis://localhost:6379/0

# Set to True to run Celery tasks synchronously (no Redis needed — useful for CI)
# CELERY_TASK_ALWAYS_EAGER=True

# Sentry — remove or leave blank to disable error tracking
SENTRY_DSN=
